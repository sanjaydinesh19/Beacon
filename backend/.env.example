# Beacon backend env vars
# These are automatically loaded from ../../../Notion Journal/.env if set there.
# Only fill this in if you want to override or run the backend independently.

NOTION_TOKEN=
NOTION_DATABASE_ID=
OPENAI_API_KEY=
GITHUB_TOKEN=
GITHUB_JOURNAL_REPO=
SCHEDULER_TIMEZONE=Asia/Kolkata
